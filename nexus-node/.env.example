# Nexus Node 环境变量
# 复制为 .env 并填入实际值

# [必填] 节点唯一标识
NODE_ID=node_001

# HTTP API 端口
LISTEN_PORT=8080

# Gossip WebSocket 端口
GOSSIP_PORT=9090

# 链上 RPC 端点
CHAIN_RPC=ws://127.0.0.1:9944

# 节点私钥路径
SIGNING_KEY_PATH=./data/node.key

# 数据目录
DATA_DIR=./data

# Bot 注册缓存（开发用）
# 格式: bot_id_hash:owner_pubkey:platform,...
# BOT_REGISTRATIONS=aabb...:ccdd...:telegram

# 节点列表（开发用）
# 格式: node_id@endpoint,...
NODE_LIST=node_001@http://localhost:8081,node_002@http://localhost:8082,node_003@http://localhost:8083

# Agent 端点（Leader 执行时回传指令）
AGENT_ENDPOINT=http://localhost:8443

# 日志级别
RUST_LOG=nexus_node=info
