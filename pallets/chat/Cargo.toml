[package]
name = "pallet-chat"
version = "0.1.0"
authors = ["Cosmos Team"]
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/memoio/cosmos"
description = "去中心化聊天功能模块 - Re-exports pallet-chat-core"

# 🆕 此 crate 现在仅作为 pallet-chat-core 的 re-export
# 所有功能实现在 pallet-chat-core 中

[dependencies]
# Re-export pallet-chat-core
pallet-chat-core = { path = "core", default-features = false }

[features]
default = ["std"]
std = ["pallet-chat-core/std"]
try-runtime = ["pallet-chat-core/try-runtime"]
runtime-benchmarks = ["pallet-chat-core/runtime-benchmarks"]

